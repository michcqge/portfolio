import React, { useEffect, useState } from "react";

const SplashScreen: React.FC = () => {
  return (
    <div className="fixed inset-0 bg-ocean flex items-center justify-center z-50">
      <span className="text-michael-white text-9xl font-bold">M</span>
    </div>
  );
};

export default SplashScreen;
